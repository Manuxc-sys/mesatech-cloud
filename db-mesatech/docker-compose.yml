version: '3.8'
services:
  db-mesatech:
    image: postgres:15-alpine
    container_name: db-mesatech
    ports:
      - "5432:5432"
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
